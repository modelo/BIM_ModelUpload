{
  "name": "upload_model",
  "version": "1.0.0",
  "description": "this is a script to upload a model",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "graphql-request": "^1.4.0",
    "axios": "^0.17.1"
  }
}
